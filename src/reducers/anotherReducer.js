import {ADD, EDIT} from "../constants";

export const anotherReducer = (state, {type, value}) => {
    switch (type) {
        case ADD: {
            return state
        }
        case EDIT: {

            return state
        }
        default: {
            return state
        }
    }
}
